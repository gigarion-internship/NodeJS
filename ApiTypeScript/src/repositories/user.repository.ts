// repositories/user.repository.ts
export interface User {
    id: number;
    name: string;
    email: string;
}

// This repository can be expanded to include database interactions in a real-world scenario
